<html>
    <a class="btn btn-primary" href="{{route('auth.login')}}">Admin Login</a>
    {{-- <a class="btn btn-primary" href="{{route('auth.register')}}">Admin Registration</a> --}}
    {{-- <a class="btn btn-primary" href="{{route('admin.dashboard')}}">Admin Dashboard</a> --}}
    <a class="btn btn-primary" href="{{route('teacher.register')}}">Teacher Registration</a>
    <a class="btn btn-primary" href="{{route('teacher.dashboard')}}">Teacher Dashboard</a>
    <a class="btn btn-primary" href="{{route('teacher.login')}}">Teacher login</a>
    <a class="btn btn-primary" href="{{route('student.register')}}">Student Registration</a>
    <a class="btn btn-primary" href="{{route('student.login')}}">Student login</a>
    <a class="btn btn-primary" href="{{route('student.dashboard')}}">Student Dashboard</a>

    <hr>
</html>